import test from 'node:test';import assert from 'node:assert/strict';import {buildDailyPicks,koreanDay} from '../lib/daily-picks.ts';
const now=Date.parse('2026-09-17T03:00:00Z');
const catalog={fetchedAt:'2026-09-17',interest:{startDate:'2026-08-17',endDate:'2026-09-13',series:[{title:'소설',data:[{period:'2026-09-07',ratio:20}]},{title:'과학',data:[{period:'2026-09-07',ratio:90}]},{title:'경제 경영',data:[{period:'2026-09-07',ratio:40}]}]}};
test('ranks actual same-week interest and rotates daily with unique books',()=>{const a=buildDailyPicks(catalog,now),b=buildDailyPicks(catalog,now+86400000);assert.deepEqual(a.items.slice(0,3).map(i=>i.topic),['과학','경제 경영','소설']);assert.notEqual(a.items[2].book.id,b.items[2].book.id);assert.equal(new Set(a.items.map(i=>i.book.id)).size,a.items.length);assert.equal(a.day,'2026-09-17');});
test('ignores older missing-week observations and reports uncovered topics',()=>{const c=structuredClone(catalog);c.interest.series[1].data[0].period='2026-08-31';c.interest.series.push({title:'에세이',data:[{period:'2026-09-07',ratio:100}]});const r=buildDailyPicks(c,now);assert.ok(r.items.every(i=>i.topic!=='과학'));assert.deepEqual(r.uncovered,['에세이']);});
test('uses Korean midnight and rejects absent trends',()=>{assert.equal(koreanDay(Date.parse('2026-09-17T15:00:00Z')),'2026-09-18');assert.throws(()=>buildDailyPicks({interest:{...catalog.interest,series:[]}},now));});
